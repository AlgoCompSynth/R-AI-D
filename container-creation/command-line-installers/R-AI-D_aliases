
# R-AI-D_aliases
if ! [[ "$PATH" =~ "$HOME/.local/bin" ]]
then
  export PATH="$HOME/.local/bin:$PATH"
fi

export EDITOR=/usr/bin/vim
export VISUAL=/usr/bin/vim
alias la='ls -FlAtr --color=auto'
alias ll='ls -Fltr --color=auto'
alias l='ls -Ftr --color=auto'
# R-AI-D_aliases

